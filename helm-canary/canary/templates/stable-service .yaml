apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.application.name }}-svc
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
    nodePort: 30080
  selector:
    app: {{ .Values.application.name }}