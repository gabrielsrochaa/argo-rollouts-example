apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.application.name }}-preview
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.application.name }}
